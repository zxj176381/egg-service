const mysql = {
  enable: true,
  package: 'egg-mysql',
};

const cors = {
  enable: true,
  package: 'egg-cors',
};

const jwt = {
  enable: true,
  package: 'egg-jwt',
};

module.exports = {
  mysql,
  cors,
  jwt,
};
